Items To Do:

- Allow user to enter as many expressions as wanted (expression string + line color)

- Allow user to rectangle select a portion of the graph which then gives a break down 
	of the values selected

- Allow user to disable / enable quadrants

- Allow user to visually see where two expressions equal each other

- Fix the ROUND error

- Add menu items

- Add toolbar items

- Allow user to save the graph expressions

- Add a report dialog

- Fill the area between two expressions (line from xval1 to xval2 only if all can connect)